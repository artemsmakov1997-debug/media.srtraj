<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>МЕДИА.СТРАЖ | Участие и Поддержка</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;700;900&family=Roboto:wght@400;500&display=swap" rel="stylesheet">

    <style>
        /* --- 1. Базовые Стили и Переменные --- */
        :root {
            --bg-body: #05070b;
            --bg-panel: #12141b;
            --bg-panel-soft: #151821;
            --border-color: #2c2f3b;

            --text-main: #ffffff;
            --text-muted: #8b8d96;

            --accent-blue: #22d3ee;
            --accent-blue-soft: rgba(34,211,238,0.25);
            --accent-cyan: #14b8a6;
            --accent-red: #ef4444;
            --accent-yellow: #eab308;
            --accent-green: #22c55e;
        }

        * {
            box-sizing: border-box;
            outline: none;
        }

        body {
            margin: 0;
            padding: 0;
            font-family: 'Roboto', sans-serif;
            background:
                radial-gradient(circle at top, rgba(15,23,42,0.55), transparent 60%),
                radial-gradient(circle at bottom, rgba(15,23,42,0.65), transparent 60%),
                var(--bg-body);
            color: var(--text-main);
            min-height: 100vh;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 40px 20px 80px 20px;
        }

        /* --- 2. HEADER (как в Методике) --- */

        header {
            position: sticky;
            top: 0;
            z-index: 20;
        }

        .header-inner {
            background: linear-gradient(
                180deg,
                rgba(15,23,42,0.96) 0%,
                rgba(15,23,42,0.94) 70%,
                rgba(15,23,42,0.88) 100%
            );
            backdrop-filter: blur(18px);
            border-bottom: 1px solid rgba(30,41,59,0.85);
            box-shadow: 0 18px 30px rgba(0,0,0,0.65);
        }

        .navbar {
            max-width: 1200px;
            margin: 0 auto;
            padding: 10px 20px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 20px;
        }

        .brand-title {
            font-family: 'Montserrat', sans-serif;
            font-weight: 800;
            font-size: 1.05rem;
            letter-spacing: 0.14em;
            text-transform: uppercase;
            color: #e5e7eb;
            white-space: nowrap;
            opacity: 0.95;
        }

        .brand-title span {
            color: var(--accent-blue);
        }

        nav {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
        }

        .nav-links {
            display: inline-flex;
            align-items: center;
            gap: 4px;
            padding: 4px 8px;
            border-radius: 999px;
            background: radial-gradient(circle at top, rgba(31,41,55,0.9), transparent 70%);
        }

        nav a {
            position: relative;
            color: var(--text-muted);
            text-decoration: none;
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 0.16em;
            padding: 7px 14px;
            border-radius: 999px;
            transition: color 0.18s ease-out, background 0.18s ease-out;
        }

        nav a::before {
            content: "";
            position: absolute;
            inset: 0;
            border-radius: inherit;
            background: radial-gradient(circle at top, rgba(34,211,238,0.2), transparent 70%);
            opacity: 0;
            transition: opacity 0.18s ease-out;
        }

        nav a span {
            position: relative;
            z-index: 1;
        }

        nav a:hover {
            color: #e5e7eb;
        }

        nav a:hover::before {
            opacity: 1;
        }

        /* здесь можно повесить .nav-active на нужный пункт, если решите,
           какая страница считается активной для блока "Участие и поддержка" */

        .nav-active {
            color: #f9fafb;
            background: rgba(15,23,42,0.9);
            box-shadow:
                0 0 0 1px rgba(45,212,191,0.7),
                0 0 18px rgba(45,212,191,0.35);
        }

        .nav-active::before {
            opacity: 1;
        }

        .support-button {
            padding: 7px 18px;
            border-radius: 999px;
            border: 1px solid rgba(248, 250, 252, 0.4);
            font-weight: 600;
            background: linear-gradient(135deg, #facc15, #f97316);
            color: #020617;
            cursor: pointer;
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 0.13em;
            box-shadow: 0 10px 26px rgba(0,0,0,0.8);
            white-space: nowrap;
        }

        .support-button:hover {
            filter: brightness(1.05);
        }

        @media (max-width: 720px) {
            .navbar {
                flex-wrap: wrap;
                justify-content: center;
                gap: 10px;
            }

            .brand-title {
                order: 1;
            }

            nav {
                order: 2;
                flex: 0 0 100%;
                justify-content: center;
            }

            .support-button {
                order: 3;
            }
        }

        /* нижняя неоновая линия под шапкой */
        .header-glow {
            height: 2px;
            width: 100%;
            background: radial-gradient(circle at center, rgba(34,211,238,0.9), transparent 70%);
            opacity: .55;
        }

        /* --- 3. Hero-блок поддержки в стиле "Методики" --- */

        .hero-support {
            max-width: 980px;
            margin: 40px auto 60px auto;
            padding: 40px 32px;
            border-radius: 24px;
            border: 1px solid rgba(55,65,81,0.85);
            background:
                radial-gradient(circle at top left, rgba(34,211,238,0.34), transparent 58%),
                radial-gradient(circle at bottom right, rgba(56,189,248,0.3), transparent 60%),
                linear-gradient(145deg, #020617 0%, #020617 40%, #020617 100%);
            position: relative;
            overflow: hidden;
            box-shadow: 0 28px 70px rgba(0, 0, 0, 0.85);
            text-align: left;
        }

        .hero-support::after {
            content: '';
            position: absolute;
            inset: 0;
            background: radial-gradient(circle at 110% -20%, rgba(255,255,255,0.1), transparent 60%);
            opacity: 0.8;
            pointer-events: none;
        }

        .hero-pill {
            position: relative;
            z-index: 1;
            display: inline-flex;
            align-items: center;
            gap: 8px;
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 0.14em;
            padding: 6px 12px;
            border-radius: 999px;
            border: 1px solid rgba(148, 163, 184, 0.7);
            color: var(--text-muted);
            margin-bottom: 16px;
            background-color: rgba(15, 23, 42, 0.9);
            backdrop-filter: blur(10px);
        }

        .hero-support h1 {
            position: relative;
            z-index: 1;
            font-family: 'Montserrat', sans-serif;
            font-weight: 900;
            font-size: 36px;
            margin: 6px 0 16px 0;
        }

        .hero-support p {
            max-width: 640px;
            font-size: 16px;
            color: var(--text-muted);
            line-height: 1.7;
            margin: 0 0 20px 0;
            position: relative;
            z-index: 1;
        }

        .hero-highlight {
            color: var(--accent-blue);
        }

        .hero-meta {
            position: relative;
            z-index: 1;
            display: flex;
            flex-wrap: wrap;
            gap: 14px;
            font-size: 13px;
            color: var(--text-muted);
            margin-top: 8px;
        }

        .hero-meta span {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            padding: 6px 10px;
            border-radius: 999px;
            background-color: rgba(15, 23, 42, 0.96);
            border: 1px solid rgba(51, 65, 85, 0.9);
        }

        @media (max-width: 720px) {
            .hero-support {
                padding: 28px 18px;
                margin-top: 26px;
            }
            .hero-support h1 {
                font-size: 28px;
            }
        }

        /* --- 4. Секции и заголовки --- */

        .section-title {
            font-family: 'Montserrat', sans-serif;
            font-weight: 700;
            font-size: 24px;
            margin-top: 10px;
            margin-bottom: 16px;
        }

        .section-subtitle {
            font-size: 14px;
            color: var(--text-muted);
            margin-bottom: 26px;
            max-width: 760px;
        }

        /* --- 5. Тарифные карточки в "методика"-стиле --- */

        .tiers-grid {
            display: grid;
            grid-template-columns: repeat(3, minmax(0,1fr));
            gap: 20px;
            margin-bottom: 50px;
        }

        @media (max-width: 1024px) {
            .tiers-grid {
                grid-template-columns: repeat(2, minmax(0,1fr));
            }
        }

        @media (max-width: 720px) {
            .tiers-grid {
                grid-template-columns: 1fr;
            }
        }

        .tier-card {
            position: relative;
            border-radius: 22px;
            padding: 22px 20px 20px 20px;
            background:
                radial-gradient(circle at bottom, var(--accent-blue-soft), transparent 60%),
                linear-gradient(145deg, #050816, #050b13);
            border: 1px solid rgba(31,41,55,0.9);
            box-shadow:
                0 26px 60px rgba(0,0,0,0.9),
                0 -8px 24px rgba(15,23,42,0.9);
            overflow: hidden;
            transition:
                transform 0.23s ease-out,
                box-shadow 0.23s ease-out,
                border-color 0.23s ease-out;
            display: flex;
            flex-direction: column;
        }

        .tier-card::before {
            content: '';
            position: absolute;
            inset: 0;
            background: radial-gradient(circle at top, rgba(148,163,184,0.35), transparent 75%);
            opacity: 0;
            pointer-events: none;
            transition: opacity 0.23s ease-out;
        }

        .tier-card:hover {
            transform: translateY(-4px);
            border-color: rgba(56,189,248,0.9);
            box-shadow:
                0 30px 80px rgba(0,0,0,1),
                0 0 40px rgba(56,189,248,0.65);
        }

        .tier-card:hover::before {
            opacity: 0.35;
        }

        .tier-label {
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 0.16em;
            color: var(--text-muted);
            margin-bottom: 8px;
        }

        .tier-title {
            font-family: 'Montserrat', sans-serif;
            font-size: 18px;
            font-weight: 600;
            margin-bottom: 4px;
        }

        .tier-subtitle {
            font-size: 13px;
            color: var(--text-muted);
            margin-bottom: 14px;
        }

        .tier-price {
            font-size: 26px;
            font-weight: 700;
            margin-bottom: 18px;
            display: flex;
            align-items: baseline;
            gap: 6px;
        }

        .tier-price span {
            font-size: 13px;
            color: var(--text-muted);
            font-weight: 400;
        }

        .features-list {
            list-style: none;
            padding: 0;
            margin: 0 0 22px 0;
            font-size: 13px;
            color: #e5e7eb;
            flex-grow: 1;
        }

        .features-list li {
            margin-bottom: 8px;
            display: flex;
            align-items: flex-start;
            line-height: 1.5;
        }

        .features-list li::before {
            content: '•';
            color: var(--accent-green);
            font-weight: bold;
            margin-right: 8px;
            margin-top: 1px;
        }

        .support-btn {
            width: 100%;
            padding: 12px;
            border-radius: 999px;
            border: none;
            font-family: 'Montserrat', sans-serif;
            font-weight: 700;
            cursor: pointer;
            transition: 0.2s;
            font-size: 13px;
            text-transform: uppercase;
            letter-spacing: 0.14em;
        }

        .btn-blue {
            background: linear-gradient(135deg, #38bdf8, #0ea5e9);
            color: #020617;
            box-shadow: 0 14px 32px rgba(8,47,73,0.85);
        }

        .btn-blue:hover {
            filter: brightness(1.05);
        }

        .btn-green {
            background: linear-gradient(135deg, #bbf7d0, #22c55e);
            color: #022c22;
            box-shadow: 0 14px 32px rgba(5,46,22,0.9);
        }

        .btn-green:hover {
            filter: brightness(1.05);
        }

        .btn-gold {
            background: linear-gradient(135deg, #facc15, #f97316);
            color: #451a03;
            box-shadow: 0 14px 32px rgba(120,53,15,0.9);
        }

        .btn-gold:hover {
            filter: brightness(1.05);
        }

        .tier-badge {
            position: absolute;
            top: 14px;
            right: 18px;
            font-size: 10px;
            text-transform: uppercase;
            letter-spacing: 0.18em;
            padding: 4px 8px;
            border-radius: 999px;
            border: 1px solid rgba(250,204,21,0.8);
            color: #fbbf24;
            background-color: rgba(23,23,23,0.9);
        }

        /* --- 6. Блок "Другие способы участия" --- */

        .secondary-wrapper {
            max-width: 960px;
            margin: 0 auto;
            padding-top: 34px;
            border-top: 1px solid rgba(31,41,55,0.9);
        }

        .secondary-title {
            font-family: 'Montserrat', sans-serif;
            font-weight: 700;
            font-size: 22px;
            margin-bottom: 14px;
        }

        .secondary-subtitle {
            font-size: 14px;
            color: var(--text-muted);
            margin-bottom: 24px;
        }

        .secondary-grid {
            display: grid;
            grid-template-columns: 1.2fr 1fr;
            gap: 24px;
        }

        @media (max-width: 880px) {
            .secondary-grid {
                grid-template-columns: 1fr;
            }
        }

        .secondary-card {
            background:
                radial-gradient(circle at bottom, rgba(34,211,238,0.16), transparent 60%),
                linear-gradient(145deg, #050816, #050b13);
            border-radius: 20px;
            border: 1px solid rgba(31,41,55,0.9);
            padding: 20px 18px;
            box-shadow: 0 20px 55px rgba(0,0,0,0.9);
        }

        .secondary-card h3 {
            font-family: 'Montserrat', sans-serif;
            font-size: 17px;
            margin-top: 0;
            margin-bottom: 10px;
        }

        .secondary-card p {
            font-size: 13px;
            color: var(--text-muted);
            margin-bottom: 14px;
            line-height: 1.6;
        }

        .one-time-input {
            display: flex;
            gap: 10px;
        }

        .one-time-input input {
            flex: 1;
            background-color: rgba(15,23,42,0.96);
            border: 1px solid rgba(51,65,85,0.9);
            padding: 10px 12px;
            border-radius: 999px;
            color: #e5e7eb;
            font-size: 14px;
        }

        .one-time-input input::placeholder {
            color: rgba(148,163,184,0.8);
        }

        .one-time-input button {
            padding: 10px 18px;
            font-size: 12px;
        }

        .contact-links a {
            display: block;
            color: var(--accent-blue);
            text-decoration: none;
            margin-bottom: 8px;
            font-weight: 500;
            font-size: 13px;
        }

        .contact-links a:hover {
            text-decoration: underline;
        }

        .contact-note {
            font-size: 12px;
            color: var(--text-muted);
            margin-top: 8px;
        }

        @media (max-width: 720px) {
            .container {
                padding-top: 28px;
            }
        }
    </style>
</head>
<body>

<header>
    <div class="header-inner">
        <div class="navbar">
            <div class="brand-title">МЕДИА.<span>СТРАЖ</span></div>

            <nav>
                <div class="nav-links">
                    <a href="index.html"><span>РЕЙТИНГ</span></a>
                    <a href="about.html"><span>О ПРОЕКТЕ</span></a>
                    <a href="method.html"><span>МЕТОДИКА</span></a>
                    <a href="archive.html"><span>АРХИВ</span></a>
                </div>
            </nav>

            <button class="support-button">ПОДДЕРЖАТЬ</button>
        </div>
        <div class="header-glow"></div>
    </div>
</header>

<div class="container">

    <!-- HERO-БЛОК ПОДДЕРЖКИ В СТИЛЕ "МЕТОДИКА" -->
    <section class="hero-support" id="support-top">
        <div class="hero-pill">
            🌐 участие · как помочь проекту
        </div>
        <h1>Развитие стоит дорого. Независимость — бесценна</h1>
        <p>
            «МЕДИА.СТРАЖ» не берёт деньги у государства и медиахолдингов, чтобы алгоритм оставался
            <span class="hero-highlight">максимально беспристрастным</span>. Регулярная поддержка даёт нам возможность
            круглосуточно считывать повестку, обновлять модели и публиковать открытые рейтинги.
        </p>
        <div class="hero-meta">
            <span>🛰 без рекламных интеграций в рейтингах</span>
            <span>📊 открытая методика и логика подсчёта</span>
            <span>🧠 развитие ИИ-модели за счёт сообщества</span>
        </div>
    </section>

    <!-- ТАРИФЫ / УРОВНИ УЧАСТИЯ -->
    <section>
        <h2 class="section-title">Форматы регулярной поддержки</h2>
        <p class="section-subtitle">
            Любой уровень участия важен. Все подписчики получают доступ к аналитике, а проект — запас прочности,
            чтобы не зависеть от чьей-то повестки.
        </p>

        <div class="tiers-grid">

            <article class="tier-card">
                <div class="tier-label">уровень 01</div>
                <div class="tier-title">Наблюдатель</div>
                <div class="tier-subtitle">Базовый доступ</div>
                <div class="tier-price">200 ₽ <span>/ месяц</span></div>

                <ul class="features-list">
                    <li>Поддержка работы серверов и сбора новостных лент.</li>
                    <li>Еженедельный дайджест главных манипуляций в медиапространстве.</li>
                    <li>Бейдж «Спонсор» в личном кабинете (когда он появится).</li>
                </ul>

                <button class="support-btn btn-blue">Поддержать · 200 ₽</button>
            </article>

            <article class="tier-card">
                <div class="tier-label">уровень 02</div>
                <div class="tier-title">Исследователь</div>
                <div class="tier-subtitle">Для тех, кто работает с данными</div>
                <div class="tier-price">500 ₽ <span>/ месяц</span></div>

                <ul class="features-list">
                    <li>Все привилегии уровня «Наблюдатель».</li>
                    <li>Ранний доступ к большим аналитическим отчётам (за 24 часа до публикации).</li>
                    <li>Расширенные фильтры и выгрузки в разделе Архив.</li>
                    <li>Приоритетный доступ к экспериментальным визуализациям индексов.</li>
                </ul>

                <button class="support-btn btn-green">Поддержать · 500 ₽</button>
            </article>

            <article class="tier-card">
                <span class="tier-badge">top-support</span>
                <div class="tier-label">уровень 03</div>
                <div class="tier-title">Медиа-Страж</div>
                <div class="tier-subtitle">Премиум-участие</div>
                <div class="tier-price">1500 ₽ <span>/ месяц</span></div>

                <ul class="features-list">
                    <li>Все привилегии уровня «Исследователь».</li>
                    <li>Доступ в закрытый Telegram-чат с командой и приглашёнными аналитиками.</li>
                    <li>Возможность предлагать темы и СМИ для приоритетного анализа ИИ.</li>
                    <li>По желанию — упоминание в списке патронов проекта.</li>
                </ul>

                <button class="support-btn btn-gold">Поддержать · 1500 ₽</button>
            </article>

        </div>
    </section>

    <!-- ДРУГИЕ СПОСОБЫ УЧАСТИЯ -->
    <section class="secondary-wrapper">
        <h2 class="secondary-title">Другие способы участия</h2>
        <p class="secondary-subtitle">
            Необязательно оформлять подписку. Можно сделать разовый взнос, предложить сотрудничество
            или помочь проекту знаниями и кодом.
        </p>

        <div class="secondary-grid">
            <article class="secondary-card">
                <h3>Разовая поддержка</h3>
                <p>Если подписка — не ваш формат, но вы хотите разово поддержать развитие «МЕДИА.СТРАЖА».</p>
                <div class="one-time-input">
                    <input type="number" placeholder="Сумма в рублях (₽)">
                    <button class="support-btn btn-blue">Отправить</button>
                </div>
                <p class="contact-note">
                    Технически это тот же платёж, что и подписка, только без автопродления.
                </p>
            </article>

            <article class="secondary-card">
                <h3>Связь и сотрудничество</h3>
                <p>Для прессы, исследовательских проектов, разработчиков и волонтёров.</p>
                <div class="contact-links">
                    <a href="mailto:contact@media-strazh.com">📧 contact@media-strazh.com</a>
                    <a href="#">✈️ Telegram-канал проекта</a>
                    <a href="#">🐙 GitHub (для разработчиков)</a>
                </div>
                <p class="contact-note">
                    Если вы делаете исследование о манипуляциях в медиа — напишите, мы поможем с данными и методикой.
                </p>
            </article>
        </div>
    </section>

</div>

</body>
</html>
